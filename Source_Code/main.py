from gui import launch_gui

#The main functions of the aplication is to launch the GUI 
#previously it was used for running the terminal menu for the user to choose the source and destination folders and the version of the 
# backup but now it is not used anymore because the GUI is more user friendly and easier to use than the terminal menu.
if __name__ == "__main__":
    launch_gui()